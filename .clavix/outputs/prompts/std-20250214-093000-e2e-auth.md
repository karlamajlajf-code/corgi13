---
id: std-20250214-093000-e2e-auth
timestamp: 2025-02-14T09:30:00Z
executed: false
originalPrompt: "continue and finish the remaining E2E failures"
---

# Improved Prompt

## Objective

Stabilize the Playwright E2E account-page flow by avoiding flaky UI logins and seeding authenticated state before navigation.

## Scope

- Update the account-page E2E helper to seed auth state via the shared `seedAuthState` fixture and navigate directly to account pages.
- Keep existing auth and `/stripe/payment-methods` stubs so the tests remain deterministic when the backend is unavailable.
- Verify the targeted Playwright spec for account pages using Nx.

## Constraints

- Use Nx to run E2E tasks (no direct `playwright` CLI).
- Do not modify production app code; limit changes to Playwright tests/fixtures.
- Avoid locale-specific selectors; rely on test IDs or seeded auth state.

## Acceptance Criteria

- `tests/e2e/specs/account-pages.spec.ts` no longer performs UI login and instead seeds auth state before navigation.
- Running `pnpm exec nx run @tejo/root:e2e --project=chromium -- tests/e2e/specs/account-pages.spec.ts` completes without failures.
- No unrelated tests or files are modified.
