---
id: std-20250202-110000-loyalty-e2e
timestamp: 2025-02-02T11:00:00Z
executed: false
originalPrompt: "Continue fixing the remaining loyalty-program E2E failures and get the suite passing."
---

# Improved Prompt

## Objective

Stabilize the remaining failing loyalty-program Playwright E2E tests so the suite passes consistently.

## Scope

- Inspect the latest loyalty-program E2E failures and focus on fixing the shared login helper and related stubs.
- Update `tests/e2e/specs/loyalty-program.spec.ts` and any required E2E fixtures to prevent login timeouts and keep API stubs aligned with the app.
- Re-run only the loyalty-program spec via Nx to confirm green results.

## Constraints

- Use Nx commands for test execution (`pnpm -s nx run ...`).
- Avoid touching production code unless necessary for test stability.
- Preserve existing test intent; make selectors/assertions tolerant but still meaningful.

## Acceptance Criteria

- `tests/e2e/specs/loyalty-program.spec.ts` runs without timeouts from the login helper.
- The loyalty-program spec passes under Chromium when executed with the existing Nx command.
- Any changes are limited to E2E fixtures/specs (unless a minimal production tweak is required).
