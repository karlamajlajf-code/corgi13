---
id: std-20260201-174000-1d8c
depthUsed: standard
timestamp: 2026-02-01T17:40:00Z
executed: true
originalPrompt: "whatever is better"
---

# Improved Prompt

Objective:
Resolve the local database port mismatch for `@tejo/api` tests by applying the best default configuration and verifying tests.

Scope:

- Align local database configuration to the canonical Postgres port used by docker compose.
- Keep changes minimal and localized to environment/config files.
- Re-run `@tejo/api:test` without extra environment overrides to confirm the fix.

Constraints:

- Use Nx commands for verification (e.g., `nx run @tejo/api:test`).
- Avoid unrelated refactors or formatting changes.

Acceptance Criteria:

- `@tejo/api:test` passes without manual `DATABASE_URL` overrides.
- The local environment references the canonical DB port consistently.

## Quality Scores

- **Clarity**: 20%
- **Efficiency**: 55%
- **Structure**: 20%
- **Completeness**: 20%
- **Actionability**: 20%
- **Overall**: 27% (needs-improvement)

## Original Prompt

```text
whatever is better
```
