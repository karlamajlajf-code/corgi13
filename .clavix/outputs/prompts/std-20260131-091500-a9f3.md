---
id: std-20260131-091500-a9f3
depthUsed: standard
timestamp: 2026-01-31T09:15:00Z
executed: false
originalPrompt: "continue"
---

# Improved Prompt

Objective:
Continue Phase 4.3 by building the admin analytics dashboard for personalization/recommendations.

Scope:

- Create a new admin page at `apps/web/src/pages/admin/personalization.tsx` with a dashboard layout consistent with existing admin pages (e.g., `admin/loyalty.tsx`).
- Add a server action to fetch personalization analytics from `GET /api/v1/personalization/analytics` with bearer token authentication.
- Add a React Query hook in `apps/web/src/lib/hooks/use-server-actions.ts` for the admin personalization analytics action.
- Display core metrics from the API response: `totalRecommendations`, `clickThroughRate`, `conversionRate`, `averageOrderValue`, plus an optional message when available.
- Provide loading and error states consistent with other admin dashboards.

Constraints:

- Follow existing patterns in `server-actions.ts` and `use-server-actions.ts`.
- Use TypeScript interfaces for analytics data in the page component.
- Keep the existing API endpoint behavior (no backend aggregation changes in this step).
- Use Tailwind classes and layout conventions from other admin pages.

Acceptance Criteria:

- Admin route `/admin/personalization` renders a dashboard with 4 metric cards and an insights/message section.
- Data is fetched via the new server action and hook using a token from `localStorage`.
- Loading state shows a spinner; error state shows a friendly message.
- Build/typecheck passes for the web app after changes.

## Quality Scores

- **Clarity**: 25%
- **Efficiency**: 65%
- **Structure**: 20%
- **Completeness**: 20%
- **Actionability**: 25%
- **Overall**: 26% (needs-improvement)

## Original Prompt

```text
continue
```
