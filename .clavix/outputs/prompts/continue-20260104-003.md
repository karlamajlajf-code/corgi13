---
id: continue-20260104-003
timestamp: 2026-01-04T00:00:00Z
executed: false
originalPrompt: "continue working"
---

# Improved Prompt

## Objective

Continue the session by triaging the (very large) dirty working tree under `tejospec/`, while keeping the canonical Nx workspace healthy and verified.

## Scope

- Confirm repo root + current SCM state under `tejospec/`.
- Produce a compact diff summary (stats + counts) and identify likely accidental artifacts (e.g. `package-lock.json`, build outputs, editor settings).
- Keep verification green:
  - `pnpm check:working-tree:strict`
  - `pnpm exec nx run @tejo/web:typecheck`
  - `pnpm exec nx run @tejo/api:typecheck`

## Constraints

- Do not discard changes automatically.
- Do not `git commit` unless explicitly requested.
- Avoid touching legacy folders (`tejospec/backend/**`, `tejospec/frontend/**`, `tejospec/archive/**`) unless needed for cleanup.

## Acceptance Criteria

- Provide an actionable working-tree triage summary with:
  - tracked diff stats (`git diff --stat`),
  - untracked file count + top-level grouping,
  - a recommended cleanup path.
- Ask one explicit “keep vs revert” question before any destructive git operations.
