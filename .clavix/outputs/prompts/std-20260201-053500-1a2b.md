---
id: std-20260201-053500-1a2b
timestamp: 2026-02-01T05:35:00Z
executed: false
originalPrompt: "does not work\n\neverything is overlaping everything\n\nsinc it with backend"
---

# Improved Prompt

## Objective

Fix the frontend layout so sections do not overlap and ensure the web app is correctly synced with the backend API. The site should render with proper stacking and spacing across all sections, and all frontend API calls should use the same backend base URL.

## Scope

- Identify and resolve any CSS/layout rules that cause sections to overlap (global styles, layout wrappers, and section containers).
- Ensure Tailwind/container overrides are applied consistently and do not break layout flow.
- Normalize backend API base URLs used in server actions and client actions so they resolve to the same backend service (local default should be the backend running on port 8003).
- Keep existing design language and component structure intact.

## Constraints

- Do not remove Tailwind v4 setup (`@import 'tailwindcss'` + `@config`), only adjust or add targeted layout rules.
- Prefer minimal, surgical changes; avoid large refactors.
- Use environment-driven API base resolution with a safe local default matching the docker-compose backend (`http://localhost:8003/api/v1`).

## Acceptance Criteria

- No visible overlap between header, hero, and subsequent sections on desktop and mobile.
- Main sections stack vertically with normal flow (no absolute/fixed positioning issues).
- Backend API calls from server-actions and related files resolve to the same base URL (no `localhost:3001` defaults).
- Page renders correctly after restarting the frontend with fresh cache.
