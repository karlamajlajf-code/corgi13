---
id: std-20260130-151500-vip-phase23
timestamp: 2026-01-30T15:15:00Z
executed: false
originalPrompt: "continue"
---

# Improved Prompt

## Objective

Complete Phase 2.3 by implementing the VIP Tier user experience and related account pages so all VIP-tier user journey E2E expectations pass.

## Scope

- Create public VIP tiers page and comparison page:
  - `apps/web/src/pages/vip-tiers.tsx`
  - `apps/web/src/pages/vip-tiers/compare.tsx`
- Create authenticated account VIP pages:
  - `apps/web/src/pages/account/vip-tier/index.tsx`
  - `apps/web/src/pages/account/vip-tier/eligibility.tsx`
  - `apps/web/src/pages/account/vip-tier/history.tsx`
- Update VIP tier components where needed for test IDs:
  - `apps/web/src/components/vip-tier/TierHistory.tsx`
- Enhance profile page with VIP badge and benefits toggle:
  - `apps/web/src/pages/account/profile.tsx`

## Constraints

- Use existing VIP components (`TierList`, `TierCard`, `TierHistory`, `TierUpgradeModal`, `UserTierDisplay`, `useUserTier`) and maintain styling conventions.
- Account pages must use `useRequireAuth` and read user data via `useAuthStore`.
- Avoid new documentation files unless explicitly requested.
- Use mock/fallback data where APIs are not available.
- Ensure all required `data-testid` attributes from E2E specs are present.
- Follow Next.js pages conventions with `getStaticProps` + `serverSideTranslations`.

## Acceptance Criteria

- `/vip-tiers` renders tier cards with `data-testid="vip-tier-card"` and supports detail modal with `data-testid="tier-detail-modal"` plus `tier-benefits-list` and `spending-requirement`.
- `/vip-tiers/compare` renders comparison table with `data-testid="tier-comparison-table"`, `comparison-tier-column`, and `comparison-feature-row`.
- `/account/vip-tier` shows `current-tier-badge`, `tier-level`, `tier-since-date`, progress UI, and benefit activation controls (`activate-benefit-button`, `benefit-status`, `benefit-activation-success`).
- `/account/vip-tier/eligibility` shows `eligibility-status`, `upgrade-cta`, and `remaining-spend`.
- `/account/vip-tier/history` shows `tier-history-timeline` with entries containing `timeline-entry`, `entry-date`, `entry-tier`, `entry-type`.
- `/account/profile` includes VIP badge (`profile-tier-badge`), a benefits toggle (`view-benefits-button`), and a list with `active-benefits-list`.
