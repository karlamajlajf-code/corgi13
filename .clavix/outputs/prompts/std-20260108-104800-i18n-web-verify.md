---
id: std-20260108-104800-i18n-web-verify
timestamp: 2026-01-08T10:48:00Z
executed: true
originalPrompt: "Continue the i18n work: stop translation key leaks, keep Croatian as primary language, and finish verification (Nx typecheck/lint) for the tejospec web app."
---

# Improved Prompt

## Objective

Finish the current i18n stabilization work in `tejospec/apps/web` by ensuring all recently edited pages compile and pass checks, while keeping Croatian (`hr`) as the primary language and preventing any raw i18n-key “leaks” in UI.

## Scope

1. **Fix compilation/type errors** introduced by the recent translation-preloading sweep (e.g., invalid `getServerSideProps` placement).
2. **Verify** the `@tejo/web` Nx project via the narrowest relevant checks:

   - `pnpm nx run @tejo/web:typecheck`
   - then `pnpm nx run @tejo/web:lint`

3. Only make changes necessary to resolve the failing checks; do not refactor unrelated areas.

## Constraints

- Use Nx for all checks (`pnpm nx run ...`).
- Keep existing i18n structure based on `next-i18next` and `getCommonTranslations(locale)`.
- Do not revert the previously added translation preload coverage; just correct mistakes (e.g., ensure `getServerSideProps` is top-level).
- Avoid introducing new untranslated hardcoded strings; prefer existing translation keys.

## Acceptance Criteria

- `pnpm nx run @tejo/web:typecheck` succeeds.
- `pnpm nx run @tejo/web:lint` succeeds.
- Any page-level SSR/SSG translation preload exports (`getServerSideProps` / `getStaticProps`) are declared at module scope (not nested), with correct imports.
- No new runtime references to browser-only APIs are introduced in SSR paths.

## Implementation Notes

- Start by addressing the current TypeScript error:
  - `tejospec/apps/web/src/pages/admin/loyalty.tsx` has a nested `export const getServerSideProps` inside a function; move it to module scope.
- Re-run `typecheck` after each fix and iterate until clean.
