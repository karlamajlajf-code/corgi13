---
id: std-20260201-023730-a9f3
depthUsed: standard
timestamp: 2026-02-01T02:37:30Z
executed: false
originalPrompt: "whole front is broken"
---

# Improved Prompt

Objective:

Fix the broken frontend rendering on the Next.js web app homepage so it displays real content instead of raw translation keys and broken layout.

Context:

- Repo: `tejospec` (apps/web)
- The homepage shows raw i18n keys like `home.hero.slides.hemaFree.discount` instead of translated text, and the layout appears broken (see screenshot).
- CSS assets are loading (global CSS chunk returns 200), so focus on translation/init/locale loading or rendering logic rather than missing CSS.

Scope:

1. Identify why i18n translation keys are rendering instead of translated strings on the homepage.
2. Fix the i18n initialization/locale loading/configuration so translated strings render correctly.
3. Ensure the homepage hero/tiles and key sections render with correct copy and layout.
4. Keep changes minimal and limited to the web app.

Constraints:

- Use the existing Next.js pages router setup in `apps/web`.
- Follow existing i18n config (next-i18next or current i18n setup).
- Avoid introducing new dependencies unless required to fix the issue.

Acceptance Criteria:

- Homepage renders with human-readable translations (no raw translation keys).
- Hero section and navigation text render correctly in the default locale.
- No translation-related runtime errors in the console.
- Layout appears normal (no overlapping/stacked key text).

Deliverables:

- Code changes in the web app that resolve the translation rendering issue.
- Brief note on what caused the issue and how it was fixed.

## Quality Scores

- **Clarity**: 40%
- **Efficiency**: 60%
- **Structure**: 35%
- **Completeness**: 30%
- **Actionability**: 35%
- **Overall**: 40% (needs-improvement)

## Original Prompt

```text
whole front is broken
```
