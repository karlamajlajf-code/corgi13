---
id: continue-20260104-002
timestamp: 2026-01-04T00:00:00Z
executed: false
originalPrompt: "continue go further"
---

# Improved Prompt

## Objective

Continue and extend verification after recent edits (Clavix tasks + web footer), keeping work aligned to the canonical Nx workspace under `tejospec/`.

## Scope

- Confirm current contents and compilation of:
  - `.clavix/outputs/tejo-beauty-enterprise-platform/tasks.md`
  - `tejospec/apps/web/src/components/layout/Footer.tsx`
- Determine SCM status via available tooling:
  - Prefer `git status` if a `.git` worktree exists.
  - If no Git metadata is present locally, rely on VS Code SCM/tooling (`get_changed_files`) to understand diffs.
- Run the next canonical verification steps:
  - `pnpm check:working-tree:strict`
  - `pnpm exec nx run @tejo/web:typecheck`
  - `pnpm exec nx run @tejo/api:typecheck`

## Constraints

- Do not edit legacy folders (`backend/`, `frontend/`, `archive/`) unless explicitly requested.
- Keep changes minimal and targeted; if verification is already green, prefer making no further edits.

## Acceptance Criteria

- All three checks pass: `check:working-tree:strict`, `@tejo/web:typecheck`, `@tejo/api:typecheck`.
- If any check fails, either:
  - Fix only failures clearly caused by the recent edits, or
  - Report the failure with a precise next action.
